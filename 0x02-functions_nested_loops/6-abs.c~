#include "main.h"

/**
 * _abs -  computes the absoluet value of an integer
 * @t: The integer to be computed
 * Retuen: The absolute value
 */
int _abs(int t)
{
if (t >= 0)
return (t);
else
return (-t);
}
